<h1>Edit Course</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.admin_course_path(@conn, :update, @course)) %>

<span><%= link "Back", to: Routes.admin_course_path(@conn, :index) %></span>
