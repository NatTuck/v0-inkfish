<h1>Admin: Listing Users</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Login</th>
      <th>Email</th>
      <th>Given name</th>
      <th>Surname</th>
      <th>Nickname</th>
      <th>Is admin</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for user <- @users do %>
    <tr>
      <td><%= user.login %></td>
      <td><%= user.email %></td>
      <td><%= user.given_name %></td>
      <td><%= user.surname %></td>
      <td><%= user.nickname %></td>
      <td><%= user.is_admin %></td>

      <td>
        <%= link "Show", to: Routes.admin_user_path(@conn, :show, user) %>&nbsp;
        <%= link "Edit", to: Routes.admin_user_path(@conn, :edit, user) %>&nbsp;
        <%= link("Delete", to: Routes.admin_user_path(@conn, :delete, user),
          method: :delete, data: [confirm: "Are you sure?"]) %>&nbsp;
        <%= link("Impersonate", to: Routes.admin_user_path(@conn, :impersonate, user),
          method: :post) %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

